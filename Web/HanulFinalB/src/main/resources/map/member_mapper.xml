<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<insert id="register">
		INSERT INTO USERS ( USER_ID
		, USER_PW
		, NAME
		, ADDRESS
		, ADDRESS2
		, EMAIL
		, PHONE)
		VALUES( #{user_id}
		, #{user_pw}
		, #{name}
		, #{address}
		, #{address2}
		, #{email}
		, #{phone})
	</insert>
	<!-- 로그인 -->
	<select id="loginPOST" resultType="com.hanul.finalb.member.MemberVO">
		select * from users where user_id = #{user_id} 
	</select>
	<!-- 아이디 찾기 -->
	<select id="searchId" resultType="String">
		select users(user_id, 0) from user_id 
        where name = #{name} and phone = #{phone} and user_key = 'Y'
	</select>
	
</mapper>