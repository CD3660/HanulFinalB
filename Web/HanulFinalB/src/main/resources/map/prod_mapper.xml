<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="prod">
	<select id="list" resultType="prodVO">
		select *
		from product
	</select>
	<select id="info" resultType="prodVO">
		select * 
		from product 
		where prod_id = ${prod_id}
	</select>
	<insert id="insert">
		insert into product
		(prod_name, detail, prod_img,
		prod_img_id, price, stock)
		values (#{prod_name}, #{detail,
		jdbcType=VARCHAR},
		#{prod_img, jdbcType=VARCHAR},#{prod_img_id,
		jdbcType=VARCHAR},#{price}, #{stock})
	</insert>
	<update id="update">
		update product
		set prod_name=#{prod_name},
		detail=#{detail, jdbcType=VARCHAR},
		price=#{price}, stock=#{stock}
		where prod_id = #{prod_id}
	</update>
	<update id="updateFile">
		update product
		set prod_img=#{prod_img, jdbcType=VARCHAR}, prod_img_id=#{prod_img_id, jdbcType=VARCHAR}
		where prod_id = #{prod_id}
	</update>
	<delete id="delete">
		delete from product where prod_id = #{prod_id}
	</delete>
</mapper>


